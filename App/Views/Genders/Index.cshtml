@model IEnumerable<App.Models.Gender>

@{
    ViewData["Title"] = "Genders";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="mb-0">Genders</h2>
        <a class="btn btn-success" asp-action="Create">Add New Gender</a>
    </div>

    <table class="table table-bordered table-striped">
        <thead class="table-success">
            <tr>
                <th>Gender ID</th>
                <th>Gender Name</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.GenderId</td>
                    <td>@item.GenderName</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.GenderId" class="btn btn-sm btn-warning me-1">Edit</a>
                        <button class="btn btn-sm btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal-@item.GenderId">Delete</button>

                        <!-- Delete Modal -->
                        <div class="modal fade" id="deleteModal-@item.GenderId" tabindex="-1" aria-labelledby="deleteModalLabel-@item.GenderId" aria-hidden="true">
                            <div class="modal-dialog">
                                <div class="modal-content">
                                    <form asp-action="Delete" asp-route-id="@item.GenderId" method="post">
                                        <div class="modal-header">
                                            <h5 class="modal-title" id="deleteModalLabel-@item.GenderId">Confirm Delete</h5>
                                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                        </div>
                                        <div class="modal-body">
                                            Are you sure you want to delete gender <strong>@item.GenderName</strong>?
                                        </div>
                                        <div class="modal-footer">
                                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                            <button type="submit" class="btn btn-danger">Delete</button>
                                        </div>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
